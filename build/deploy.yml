trigger:
  branches:
    include:
      - main

stages:
  - stage: Plan
    displayName: "Terraform Plan"
    jobs:
      - job: Plan
        displayName: "Terraform Plan"
        pool:
          vmImage: "ubuntu-latest"
        steps:
          - script: |
              echo "Hello world"
            displayName: "Run Hello World"
